LoadScene
Use:
Open the scene Scene_A, play it, click the button 'Load B'.

Make sure the Scene_A and the Scene_B are both added to
the 'Scenes In Build' field of the build settings.

Important features used (see the ExLoadScene_A and ExLoadScene_B graphs):
1. Use a UI button.
2. Load scene.